services:
  consensus:
    ports:
      - 127.0.0.1:${CL_RPC_PORT:-26657}:${CL_RPC_PORT:-26657}/tcp
      - 127.0.0.1:${CL_REST_PORT:-1317}:${CL_REST_PORT:-1317}/tcp
      - 127.0.0.1:${CL_GRPC_PORT:-9090}:${CL_GRPC_PORT:-9090}/tcp
